<!--miniprogram/custom-tab-bar/index.wxml-->
<view class="tab-bar">
    <view class="tab-bar-border"></view>
    <view wx:for="{{list}}" wx:key="index" class="tab-bar-item" data-path="{{item.pagePath}}" data-index="{{index}}" bindtap="switchTab">
      <!-- <image hidden="{{index == 2}}" src="{{selected === index ? item.selectedIconPath : item.iconPath}}"></image> -->
      <image src="{{selected === index ? item.selectedIconPath : item.iconPath}}"></image>
      <view style="color: {{selected === index ? selectedColor : color}}">{{item.text}}</view>
    </view>
    <!-- <view class="show-img-pad"><image bindtap="toShare" src="{{selected == 2 ? 'https://qizhifan.oss-cn-hangzhou.aliyuncs.com/yoga/system/merchant_applet/20211018/share-icon.png' : 'https://qizhifan.oss-cn-hangzhou.aliyuncs.com/yoga/system/merchant_applet/20211018/share-icon.png'}}" class="big-img big-style"></image></view> -->
  </view>
  